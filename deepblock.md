目标：研究开发一个加密数据块检测算法，要求能够准确检测加密数据块和高压缩数据块，准确率>90%。
检测数据块大小：1KB、2KB、4KB、8KB、16KB、32KB、64KB、128KB、256KB、512KB、1MB、2MB、4MB、8MB
数据路径：/Users/tanfeng/code/data
数据处理方式：1、数据路径中有两个大文件数据data=[0.bin, 1.bin]，分别是压缩数据和加密数据，后续所有算法设计、模型训练、测试都基于这个数据进行；2、按4:1的比例划分data=[0.bin, 1.bin]，得到训练数据train=[0.bin, 1.bin]和测试数据test=[0.bin, 1.bin]，训练数据用于算法设计、模型训练、验证，测试数据用于最终的模型测试，测试数据上的准确率就是最终的目标准确率。
数据构造解释（供参考）：1、首先构造了5000个*.7z压缩文件；2、遍历并二进制读取每个*.7z文件，拼接存入一个0.bin文件中，读取每个*.7z文件时仅读取去掉头2KB和尾2KB元数据的中间数据部分；3、遍历读取每个*.7z文件的同时，对读取的数据利用AES加密算法进行加密，将加密后的数据拼接存入一个1.bin文件中；4、最后得到数据data=[0.bin, 1.bin]。
可选算法：包括但不限于熵值、卡方检验值、随机性测试组合、神经网络等。
重要提醒：这个任务很难，如果测试准确率不满足目标要求，需要反复尝试研究所有可能的方法，以达到目标准确率>90%。